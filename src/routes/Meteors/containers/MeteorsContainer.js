import { connect } from 'react-redux'

import Meteors from '../components/Meteors'

const mapDispatchToProps = {

}

const mapStateToProps = (state) => ({

})

export default connect(mapStateToProps, mapDispatchToProps)(Meteors)
